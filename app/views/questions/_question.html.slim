div id="question-#{question.id}"
  h4= link_to question.title, question
  p= render 'votes/vote', resource: question
  p= link_to 'Delete', question_path(question), method: :delete, remote: true, local: false if current_user == question.author
