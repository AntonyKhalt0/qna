- if current_user.subscribed?(question)
  = link_to 'Unsubscribe', question_subscription_path(@subscription), method: :delete, remote: true
- else
  = link_to 'Subscribe', question_question_subscriptions_path(question), method: :post, remote: true
